# Archived Design Material

This folder contains earlier, broader design work that is not the active implementation scope.

Why it was archived:

- the earlier design explored a larger orchestration platform
- the current product direction is intentionally smaller
- archived material may contain useful later ideas, but it should not drive MVP implementation

Current active implementation document:

- `docs/mvp-build-spec.md`

Archive usage rule for AI agents:

- do not pull requirements from this archive unless the active spec explicitly references them
- if a future change wants to revive an archived idea, update the active spec first
